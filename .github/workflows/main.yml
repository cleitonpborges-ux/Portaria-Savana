name: Gerar APK Portaria Savana
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4.2.2

      - name: Configurar Java (JDK 17)
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Configurar Node
        uses: actions/setup-node@v4.2.0
        with:
          node-version: '24'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Instalar Bubblewrap
        run: npm install -g @bubblewrap/cli

      - name: Criar twa-manifest.json
        run: |
          cat > twa-manifest.json << 'EOF'
          {
            "packageId": "io.github.cleitonpborges_ux.portaria_savana",
            "host": "portaria-savana.web.app",
            "name": "Portaria Savana",
            "launcherName": "Portaria",
            "display": "standalone",
            "themeColor": "#0f2040",
            "navigationColor": "#0f2040",
            "navigationColorDark": "#0f2040",
            "navigationDividerColor": "#0f2040",
            "navigationDividerColorDark": "#0f2040",
            "backgroundColor": "#0f2040",
            "enableNotifications": true,
            "startUrl": "/index.html",
            "iconUrl": "https://portaria-savana.web.app/icons/icon-512.png",
            "maskableIconUrl": "https://portaria-savana.web.app/icons/icon-512.png",
            "monochromeIconUrl": "https://portaria-savana.web.app/icons/icon-192.png",
            "appVersion": "2",
            "appVersionCode": 2,
            "signingKey": {
              "path": "./portaria-savana.keystore",
              "alias": "portaria"
            },
            "splashScreenFadeOutDuration": 300,
            "enableSiteSettingsShortcut": true,
            "isChromeOSOnly": false,
            "isMetaQuest": false,
            "fullScopeUrl": "https://portaria-savana.web.app/",
            "minSdkVersion": 21,
            "orientation": "portrait",
            "fingerprints": [],
            "alphaDependencies": { "enabled": false },
            "generatorApp": "bubblewrap-cli",
            "webManifestUrl": "https://portaria-savana.web.app/manifest.json",
            "serviceWorkerUrl": "https://portaria-savana.web.app/sw.js",
            "shortcuts": []
          }
          EOF

      - name: Configurar Bubblewrap com SDK do Actions
        run: |
          mkdir -p ~/.bubblewrap
          cat > ~/.bubblewrap/config.json << EOF
          {
            "jdkPath": "$JAVA_HOME",
            "androidSdkPath": "$ANDROID_SDK_ROOT"
          }
          EOF

      - name: Restaurar Keystore
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > portaria-savana.keystore

      # 🚨 PASSO FORCE-STOP: Vai parar a build aqui mostrando seu SHA-256 🚨
      - name: MOSTRAR SHA-256 DO ASSETLINKS
        run: |
          echo "=== SEU SHA-256 ESTÁ LOGO ABAIXO ==="
          keytool -list -v -keystore ./portaria-savana.keystore -storepass "${{ secrets.KEYSTORE_PASSWORD }}" -alias "portaria" | grep SHA256
          echo "===================================="
          exit 1

      - name: Gerar projeto Android
        run: |
          sudo apt-get install -y expect
          expect << EOF || true
          spawn bubblewrap update
          expect "*(Y/n)*"
          send "y\r"
          expect eof
          EOF

      - name: Injetar permissão POST_NOTIFICATIONS no AndroidManifest
        run: |
          MANIFEST="app/src/main/AndroidManifest.xml"
          if [ -f "$MANIFEST" ]; then
            if ! grep -q "POST_NOTIFICATIONS" "$MANIFEST"; then
              sed -i 's|<uses-permission android:name="android.permission.INTERNET"/>|<uses-permission android:name="android.permission.INTERNET"/>\n    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>|' "$MANIFEST"
            fi
          fi

      - name: Suprimir notificação persistente do Chrome (TWA)
        run: |
          MANIFEST="app/src/main/AndroidManifest.xml"
          if [ -f "$MANIFEST" ]; then
            if ! grep -q "FOREGROUND_SERVICE" "$MANIFEST"; then
              sed -i 's|<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>|<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>\n    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>|' "$MANIFEST"
            fi
            if ! grep -q "foregroundServiceType" "$MANIFEST"; then
              sed -i 's|android:name="org.chromium.webapk.shell_apk.HostBrowserLauncherService"|android:name="org.chromium.webapk.shell_apk.HostBrowserLauncherService" android:foregroundServiceType="dataSync"|g' "$MANIFEST"
            fi
          fi

      - name: Construir APK não assinado
        run: |
          printf "n\n" | bubblewrap build --skipSigning 2>/dev/null || true

      - name: Assinar APK com apksigner
        env:
          KS_PASS: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_PASS: ${{ secrets.KEY_PASSWORD }}
        run: |
          APK=$(find ./app/build/outputs/apk/release -name "*.apk" | head -1)
          $ANDROID_SDK_ROOT/build-tools/34.0.0/apksigner sign \
            --ks ./portaria-savana.keystore \
            --ks-pass env:KS_PASS \
            --key-pass env:KEY_PASS \
            --ks-key-alias "${{ secrets.KEY_ALIAS }}" \
            "$APK"
          cp "$APK" ./portaria-savana-signed.apk

      - name: Salvar APK assinado para Download
        uses: actions/upload-artifact@v4.6.0
        with:
          name: App-Portaria-Savana
          path: ./portaria-savana-signed.apk
